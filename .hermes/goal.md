# Goal — Migrar C# para .NET 10 e Scalar

## Estado

Concluído.

## Resultado

- Todos os projectos C# da solução Admin Shell passaram para `net10.0`.
- A documentação da API agora usa OpenAPI nativo + Scalar.
- A CI foi atualizada para .NET 10.
- A documentação foi atualizada para `net10.0`, `/openapi/v1.json` e `/scalar`.

## Endpoints verificados

- `/api/health`
- `/openapi/v1.json`
- `/scalar`
